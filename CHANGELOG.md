# 更新日志 / Changelog

## v1.6.0 (2025-08)

### ✨ 新功能
- **消息编辑 / 删除 / 重新生成（三个按钮）**：
  - 每条 AI 与用户消息下方新增 **编辑 / 删除 / 重新生成** 按钮
  - **编辑**：整会话复制 + 原地替换文本；AI 消息编辑保留推理块、工具调用与后续回合，用户消息编辑**不会**触发 AI 重新生成
  - **删除**：仅删除目标消息；删除 AI 消息会连同该回合的流式块、空消息骨架与工具调用记录一并移除，不留残留；不重新生成、不影响后续回合
  - **重新生成**：从**被点击的那条用户消息**开始重新生成（而非总是最后一条）
  - 所有操作基于 DSH append-only 日志 fork 出新会话版本，源会话自动归档；操作后自动跳转新会话
- 消息与按钮的绑定改用 DSH 官方 DOM 身份标识（`data-chat-anchor-key`：消息 id / 回合号）精确索引，替代文本匹配，杜绝 markdown 渲染差异导致的错绑

### 🐛 修复
- 删除 / 编辑 / 重发后前端自动切换到新会话（列表刷新后自动重试，仍失败则刷新页面）
- 调用过工具的 AI 消息（含空消息骨架）现在能正常显示按钮
- 操作失败时弹出服务端错误提示，不再静默无响应


## v1.5.0 (2025-08)

### ✨ 新功能
- **输入栏加号动作栏（手机端）**：输入栏左下角原生加号（直接开命令菜单）替换为新加号按钮，点击弹出底部选择栏：**命令** / **上传文件**；桌面端保持原样
- **多模型网页搜索**：
  - 设置 → 插件 → 插件配置 → **网页搜索**：配置多个搜索服务，类型支持 **Exa / Brave / Bing / Tavily / Firecrawl / DeepSeek**，每个服务填自己的 API Key
  - 模型选择器 root 面板新增 **搜索模型** 入口（和"模型 / 推理等级"并列），点击原地切换搜索模型列表（对齐推理等级面板样式），选中的搜索模型即刻生效
  - 搜索模型清单存于 `~/.dsh/dshm-search-config.json`，重启后保留；组合层 `web.searchProvider` 切到 `dshm-search`

### 🐛 修复
- 网页搜索卡片重写为显式渲染，确保"添加"表单始终可见
- 各搜索服务（无 model 概念的）不再被误报"至少需要一个模型名"
- 搜索模型入口在模型/推理等级面板正确消失、点击后不关闭弹层等多项交互修复


## v1.4.0 (2025-08)

### 🐛 修复
- **修复 v1.4.0 首次发布无法启动**：`/api/dshm/tools-config` 被注册成 GET、POST 两条同名 exact 路由，而 webserver 的路由表只按路径判重（"duplicate exact route"），导致插件树启动失败。已合并为单条路由、handler 内按方法分发；另加进程级双挂载守卫（重复挂载自动跳过，不再拖垮插件树）

### ✨ 新功能
- **工具管理页（设置 → 工具）**：
  - **搜索栏**：按工具名称/描述实时过滤，快速定位工具
  - **启用/禁用开关**：每个工具右侧的开关可随时停用或恢复；被禁用的工具对模型不可见、无法被调用
  - 禁用清单保存在 `~/.dsh/dshm-tools-config.json`，重启 DSH 后依然生效；新会话自动应用

---


### 🐛 修复
- **文件上传不再清空已输入文字**：选文件后，文件路径追加在已有文字之后（自动清理触发用的尾随 `/`），而不是整段替换草稿

---

## v1.3.0 (2025-08)

### ✨ 新功能
- **供应商余额显示**：模型选择器中，供应商标题右侧显示剩余余额（DeepSeek `¥` / OpenRouter `$` / OpenAI `$`）；无公开余额 API 的厂商（Google、Anthropic、免费试用等）自动不显示
- **文件上传给 AI**：输入 `/` → 菜单"上传文件" → 选择手机上的任意文件 → 上传到当前会话工作目录 → 输入框只显示文件路径（不自动发送，可补充说明后手动发送）
- **纯 UA 移动端检测**：仅凭浏览器标识（Android/iPhone/iPod/iPad/Windows Phone/Mobile）判断手机/电脑——桌面模式想用电脑布局就由浏览器标识决定

### 🐛 修复
- **DeepSeek 余额不显示**：余额接口返回字符串数字，解析已兼容（`Number()` 转换）
- **余额不注入**：供应商 id 可能是 `deepseek-official` 等变体，改按名称包含匹配
- **宿主上传路由丢失**：修复文件同步导致的旧版覆盖

---

## v1.2.0 (2025-08)

### ✨ 新功能
- **AI 聊天气泡**：设置 → 通用 → 可开启/关闭，AI 回复以聊天气泡样式显示（浅金色，与用户气泡区分）
- **消息头像**：设置 → 通用 → **用户头像 / AI 头像**独立开关，各自可上传自定义头像、恢复默认（用户默认人像图标、AI 默认机器人图标）
- **圆形头像裁剪**：上传头像时弹出圆形裁剪窗口（拖动定位 + 缩放），所见即所得
- **气泡与头像布局**：头像显示在气泡外侧（用户头像贴最右、AI 头像在左），AI 气泡宽度自动让位

### 🐛 修复
- **深色模式气泡不变色**：`data-dshm-ai-bubble` 标记挂在 `<html>` 导致 `body[...]` 选择器不匹配，改挂 `<body>` 后深色规则生效（深色模式为暖深金，与用户中性深灰气泡可区分）
- **头像被气泡包裹 / 用户头像未贴边 / AI 内容右溢出**：头像改为气泡外侧绝对定位 + 边距让位方案

---

## v1.1.0 (2025-08)

### ✨ 新功能
- **聊天背景**：设置 → 通用 新增"聊天背景"——选择图片并手动裁剪（拖动定位 + 缩放）作为聊天背景，支持随时"调整"（保留原图重新调参）与"移除"
- **背景模糊 / 毛玻璃滑条**：裁剪窗口新增"背景模糊"（0–20px，应用时烘焙进图片）与"毛玻璃"（0–80% 透明度）可拖动滑条，预览实时显示
- **毛玻璃界面**：设置背景后，顶部引导栏与输入框卡片（含底部统计栏）变为毛玻璃效果，浅色 / 深色模式自适应
- **背景即时移除**：移除背景立即生效，无需刷新

### 🐛 修复
- **切换会话后背景与毛玻璃消失**：会话根元素在切换时会重挂载导致内联样式丢失，现通过会话监听（useSessions）+ DOM 观察（MutationObserver）自动恢复
- **深色模式下按钮文字不可见**：`--dsw-alias-brand-primary` 在深色模式翻转为浅色，导致"白底白字"；改为静态深色令牌，深色模式下按钮保持黑底白字
- **英文 / 中文本地化缺失**：补全插件全部界面文字的英文本地化（☰ 提示、统计栏内容、回车设置、聊天背景设置、裁剪窗口等），接入应用语言系统，切换语言即时生效
- **英文排版重叠**：设置行在英文长文本下按钮与文字重叠，改为自动换行布局
- **☰ 按钮消失**：语言订阅误用未绑定类方法导致组件渲染崩溃，修复方法绑定

### 其他
- 桌面端（≥1024px 非移动设备）与原版保持完全一致

---

## v1.0.0 (首次发布)

- 📱 手机端抽屉式布局：侧栏隐藏、对话区占满全屏，☰ 滑出侧栏、点遮罩关闭
- 🖥 桌面端与原版完全一致
- 📊 底部对话统计折叠为"一行预览 + 展开箭头"
- ⌨️ 设置 → 通用 → 手机端回车键：回车 = 发送消息 / 插入换行
- ⏱ 消息元信息（时间 · 用时 · TTFT · tok/s）紧凑竖线分隔、完整显示
- ⚙️ 设置页手机端全屏显示、导航横向排列
